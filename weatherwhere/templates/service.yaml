apiVersion: v1
kind: Service
metadata:
  name: {{ include "weatherwhere.fullname" . }}
  labels:
    {{- include "weatherwhere.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weatherwhere.selectorLabels" . | nindent 4 }}
